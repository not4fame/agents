/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Example of making other env vars available (not strictly needed for NEXT_PUBLIC_)
  // env: {
  //   CUSTOM_ENV_VAR: process.env.CUSTOM_ENV_VAR,
  // },
}

module.exports = nextConfig
